# 需求复核与修复记录

## 当前修复状态 · v22

复核发现的 R1–R10 均已修复，当前 **46 / 46 项隔离回归测试通过**。同时补齐了来源编辑引发的关联任务失效、旧名称/资料回滚，以及秒级时间跨隐藏时段的边界问题。浏览器实际交互与 Edge PWA 系统级验收仍未完成，不能把这些测试等同于跨平台验收。

| 问题 | 修复结果 |
| --- | --- |
| R1 历史日期 | 编辑保留原记录日期；撤销、重做、重新加载及旧字段保留均有测试。 |
| R2 统计遗漏 | 手工实际记录与计时记录统一纳入统计、月历、热力图；父子学科同时选择不重复累计，计划记录不混入。 |
| R3 时长同步 | 修改或删除片段后重算任务总时长；已结算复习同步所属任务的时长，不覆盖同轮复习后来重新完成的结果。 |
| R4 学科与名称 | 任务学科同步全部片段和接续记录；学习来源变化同步暂停复习的分类与名称，保留本轮进度、笔记、摘要，不将旧资料覆盖回来源。 |
| R5 时间精度 | 表单增加秒字段，保留毫秒精度，支持结束时间 24:00:00；仅改名称不改起止值。隐藏时段以实际区间判断，详情和历史列表显示秒级时间。 |
| R6 日报虚增 | 日报总时长、占比、月历均使用实际秒数，不再给每段强制补足一分钟。测试捕获真实绘制函数输出。 |
| R7 跨日顺延 | “今”按钮、页面恢复、持续前台运行的检查均补查跨日顺延；多次触发幂等，不强制离开正在查看的历史日期。 |
| R8 孤立复习 | 删除来源或编辑来源导致取消复习时，同步清理相关未完成状态，保留已记录片段；有关联计时运行时拒绝编辑/删除，先提示暂停。 |
| R9 文字配置 | 保存时不重复覆盖自定义外观，绘制优先使用事件字号/透明度，正确应用加粗开关；重载后保持。 |
| R10 安装文案 | Manifest 描述已移除闪卡。 |

验证：46 项回归测试通过；app.js、sw.js 语法检查通过；页面、Service Worker、缓存资源版本一致，资源路径存在；`git diff --check` 通过。资源版本已更新为 v22，刷新原页面加载。

本次没有读取或改写用户浏览器的真实学习数据，也没有改动原 Excel。修复会保护后续操作；若旧版本已经把某条历史日期改错，程序无法可靠推断其原日期，不会擅自批量回填，需要按已知记录或备份核对。

以下保留 v21 修复前的复核证据与旧行号，便于追溯，并非当前未修复清单。

## 修复前复核报告 · v21

依据：桌面《学习日志网页搭建需求清单.xlsx》主表 A1:H27（26 条需求），当前 v21 工作区代码。原表最后修改时间为 2026-08-27 15:33:30；本轮未修改原表、生产代码、现有测试或用户数据。

### 修复前结论

**尚不能认定全部验收通过。** 原有 20 项隔离测试全部通过，但额外边界复核发现 9 项功能问题及 1 项安装说明残留。此前 `REQUIREMENTS_STATUS.md` 的“已实现”表示存在实现代码，不应继续解读为所有路径均已通过验收；本报告补充其遗漏。

本轮仅检查和报告，没有实施下列修复。优先处理历史日期变化、计时段编辑同步和统计口径。

### 修复前已复现的功能问题

| 编号 / 优先级 | 复现与实际结果 | 影响与证据 |
| --- | --- | --- |
| R1 / P1 | 当前查看 8 月 27 日，从学科历史记录或遗留详情进入 8 月 20 日记录，只改名称保存；日期变为 8 月 27 日。 | 历史记录被移至错误日期，影响时间轴、统计及复习来源。`app.js:488` 保存时固定取当前查看日，未保留既有记录日期；详情编辑入口见 548。 |
| R2 / P2 | 手工录入三级学科 09:00–10:00 学习记录，学科路径正确且时长为 3600 秒；一级和二级统计均显示 0 项 / 0m，热力图源记录也为空。 | 007 的统计一致性仍有缺口。`app.js:727`、742、797 仅接收旧 focus 类型或带 focusSeconds 的计时记录，漏掉普通手工 record/learn；030 的圆点样式已实现，但此数据筛选问题使手工记录不着色。 |
| R3 / P2 | 09:00–09:10 暂停后，把结束时间改成 09:20；箭头为 20 分钟，事件 focusSeconds 和任务 totalSeconds 仍为 600 秒。 | 026/046 的分段累计与编辑结果不一致。`app.js:488`–496 更新起止时间但未重算事件时长、任务总时长。 |
| R4 / P2 | 数学任务暂停后，把片段改成已关闭复习的英语；记录显示英语，任务仍显示数学，最终完成仍生成 5 次复习。 | 任务与记录学科不同步，复习生成规则仍使用旧学科。`app.js:496` 仅同步 task.fields；932 根据 task.categoryId/subjectPath 判断复习开关。 |
| R5 / P2 | 23:59–24:00 分段打开编辑后，结束被预填成 23:59，不能保存；23:55–24:00 段会被截短一分钟。09:00:05–09:00:25 的 20 秒段也会变成起止都 09:00，仅改名称亦无法保存。 | 026/046 新增精确计时分段不能可靠编辑。`app.js:226` 把分钟四舍五入并限制在 1439，475 将其用于编辑表单，483/487 仅接受整数分钟的有效区间。 |
| R6 / P2 | 十段各 30 秒的计时，实际共 5 分钟，分享日报却显示 10 分钟；两段各 1 秒会被计为 120 秒。 | 046 暂停分段后的日报总时长及学科占比失真。`app.js:1068`、1071 对每段应用 Math.max(1, endSlot-startSlot)，强制至少计一分钟。 |
| R7 / P2 | 8 月 27 日页面保持打开至次日，恢复页面并点击“今”；原 8 月 27 日、29 日的两轮复习仍停留原日期，没有移到 28 日、30 日。徽标显示 1 项待复习，当日列表却是 0 项。 | 002 自动顺延仅在重新加载/恢复数据等路径执行。`app.js:955` 的恢复页面处理未调用 carryOverReviews；日期按钮亦未补查，593 当日筛选排除逾期项。 |
| R8 / P2 | 完成学习 → 开始相关复习 → 暂停 → 删除原学习来源箭头；未完成列表仍保留这项复习，点击继续却提示本次复习已结束。 | 046 关联删除留下不可接续的未完成任务。`app.js:515` 删除来源复习时未同步关联 focusTasks，894 因复习不存在而拒绝继续。可手动放弃清理，但状态应自动一致。 |
| R9 / P3 | 单事件字号设 24，学科字号为 12 且关闭加粗，时间轴事件名称仍使用 700 12px。 | 时间轴已有文字配置部分失效。`app.js:326` 优先读取学科字号、328 把名称字重写死为 700。032 的选行算法已存在，但不代表既有文字配置都正常。 |

优先级：P1 应优先修复的数据错误；P2 为功能/数据一致性缺陷；P3 为较低优先级配置或文案问题。

### 修复前低优先级文案残留

R10 / P3：`manifest.webmanifest:4` 的安装描述仍写“闪卡”。本轮未发现 app.js、index.html、styles.css 中仍可使用的闪卡入口或计算逻辑；这个残留是安装元信息问题，不能与“闪卡仍可访问”混为一谈。

### 修复前需求覆盖判断

- 002、007：存在上表明确缺口，不能视作完整验收通过。
- 026、046：暂停、继续、明确完成、放弃的基本流程通过原测试；编辑分段、统计展示、关联删除边界仍需修复。
- 030：20×20、空白日期白色及颜色深浅规则有实现；手工记录漏算见 R2。
- 032：选取箭头空余最大行、同小时折叠、无后项换行及点击区域的代码与隔离测试通过；真实字体渲染、长文本布局及移动端交互未完成浏览器验收。
- 034、039：清单统一读取事件名称/资料定位、移除建议/摘要/遗留、掌握程度背景颜色的代码及已有测试通过；旧知识点仅为显示兼容来源。
- 042：运行功能已移除、旧数据在既有测试中保留；安装描述尚有 R10。
- 016、017、023：补测紧凑视图开启/刷新/关闭还原、普通保存字号、重载配置后再次进入沉浸应用字号，均通过。
- 001、003、004、005、006、009、010、027、028：复核到对应实现，除 R9 所述既有字体配置问题外，未发现其他确定的新缺口；不等同于真实浏览器全场景验收。
- 018、019：到期补查及悬浮小窗代码保留，但本轮未进行 Edge 安装 PWA、后台冻结、系统通知或实际小窗测试。
- 011 被 042 下线闪卡要求覆盖；013 的独立知识点部分被 039 覆盖，排期位于详情底部仍保留。

### 修复前验证证据与限制

1. `node --test tests/requirements.test.cjs`：20 / 20 通过。
2. 追加 4 项临时隔离边界核验：紧凑视图还原、倒数日字号、配置重载后进入沉浸 3 项通过；跨日恢复复习顺延 1 项失败，见 R7。
3. 其余功能问题通过运行真实 app.js 的内存状态/表单逻辑或绘制计算探针复现，并核对相关代码；未访问用户浏览器存储。
4. `node --check app.js`、`node --check sw.js`、`git diff --check` 通过。
5. 浏览器实测仍受此前访问策略限制。本轮未绕过限制，也未声称完成真实页面点击、视觉、移动端或 Edge PWA 系统级验收。模拟 DOM 和绘制数据不能替代这些验收。
6. 本轮仅新增本报告，原始 Excel、生产代码、既有测试和真实学习数据未改变。

原表 SHA-256：`2F94778C38363F38FACEDD76E1A0786D6DD8B82EFE25255F59C0D4172FCDFEE9`。
