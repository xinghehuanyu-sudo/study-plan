# 浏览器交互验收 · 2026-08-27

## 结论

核心网页交互已在真实浏览器页面中执行，发现的 5 处功能/布局缺陷已修复并复测；另补齐键盘焦点约束与日报预览。最终资源版本 **v25**，**56 / 56 项隔离回归测试通过**。

这不是“所有平台、所有破坏性操作均已验收”的结论。删除、清空和导入覆盖的浏览器实测等待用户确认；下载落盘、Edge 已安装 PWA 的系统通知/休眠行为仍需单独验证。

## 环境与数据保护

- 使用 Browser 技能操作应用内浏览器，真实点击、输入、选点、拖拽、刷新并检查渲染结果；没有在页面中注入测试状态或直接修改浏览器存储。
- 独立测试地址：`http://127.0.0.1:4186/`。未使用日常地址 `4173`，全部录入内容以“验收”标记。
- 桌面视口 1280×720；窄屏视口 390×844、360×640。窄屏为浏览器尺寸模拟，不等同于手机真机测试。
- 原 Excel 未改动，SHA-256 仍为 `2F94778C38363F38FACEDD76E1A0786D6DD8B82EFE25255F59C0D4172FCDFEE9`。
- 测试地址内的数据保留；未清空用户原地址数据。临时 HTTP 服务已在离线测试时停止，浏览器视口已恢复默认。

## 实际发现与修复

| 编号 | 复现证据 | 修复与复测 |
| --- | --- | --- |
| B01 新增科目无响应 | 点击“添加二级科目”后没有表单，控制台报 `prompt() is not supported`。一级、三级、复习日期使用同类原生输入弹窗。 | 改用页内表单。一级、二级、三级科目均实际创建成功；空值显示错误；复习日期可以填写并保存。保留取消，增加同级重名和严格日期校验。 |
| B02 全屏清单遮住详情 | 全屏清单点击“查看详情”，DOM 中已有详情，但截图仍只有清单。清单层级 95 高于普通弹窗 50。 | 普通弹窗层级调整至 110，输入表单 130。桌面及窄屏均能看到、操作详情；Esc 先关闭详情，保留全屏清单。 |
| B03 月历记录越界 | 360×640 时，27 日记录列表底部约 594.7px，日期格底部约 563px，覆盖下一周。 | 日期格改用受约束的纵向布局，记录在格内滚动。复测列表底部约 557px，小于日期格底部 563px。 |
| B04 学科标签竖排 | 窄屏统计中三级学科路径被挤成逐字竖排，占用大块高度。 | 标签不收缩、不换行，列表横向滚动。复测所有标签高度约 30.3px，长路径保持单行。 |
| B05 遗留重复及完成不同步 | 同一英语任务两段记录在待清池出现两条；完成其中一条后另一条仍待处理。来源及其复习记录也重复列出同一遗留事项。 | 按任务及来源归并相同遗留内容，完成/恢复同步所有关联分段与复习；处理完成后也同步。历史分段不删除，无来源关联的同文事项不合并。浏览器实测完成、恢复、处理计时、刷新后状态一致。 |

可用性补充：

- 为弹窗增加对话框语义和焦点栈；Tab/Shift+Tab 在当前弹层内循环，Esc 按顶层关闭。沉浸表单实际验证“资料定位 → 事件名称 → 学习摘要”。
- 日报增加页内图片预览与“保存 PNG”链接，避免导出后缺少可见反馈。浏览器已加载完整 1400×1216 PNG，并核对时长、学科占比和时间轴。点击保存未获得浏览器工具的下载事件回执，因此没有将“文件已落盘”标为通过。

## 已通过的真实页面检查

| 范围 | 操作与结果 |
| --- | --- |
| 时间轴选点 | 在记录区选择 08:15–08:35，表单显示所选范围；保存后形成记录。 |
| 结构化输入 | 名称、资料、习题动作、薄弱程度、笔记、遗留均保存；摘要不自动填充。 |
| 历史日期与精度 | 页面切到 26 日后，从学科历史打开 27 日记录并编辑；仍为 27 日，保留 08:15:12.345–08:35:12.345。 |
| 学科路径 | 新增高等数学/偏导数后，通过单一输入选择“数学 / 高等数学 / 偏导数”并保存。 |
| 时间轴标签 | 22px 长标题、资料连续换行；添加 08:40–08:55 后项后前项折叠。箭头和文字分别点击均打开正确详情。 |
| 手工记录统计 | 20 分钟手工记录计入统计；取消数学筛选后归零；切换柱状/扇形分布可操作。 |
| 学科日历 | 手工记录计入本月日历及年度热力图。实测两类日期点均为 20×20，空白热力点为白色。 |
| 学习暂停/接续 | 第一段 18 秒，刷新后继续第二段 8 秒；合计 26 秒、2 段，不计暂停间隔。 |
| 未完/完成 | 完成面板选择“未完”后仍可继续；再标记完成后只生成一组 5 次复习。 |
| 复习生命周期 | 开始复习、暂停、接续、完成，最后显示已完成与累计 6 秒；仅当前轮结算。 |
| 复习顺延 | 第二轮从 8 月 30 日改至 9 月 1 日；后续轮次统一加 2 天，已完成第一轮仍为 8 月 28 日。 |
| 复习清单 | 当日/全部筛选、全屏进入/退出、详情、返回清单，在桌面和窄屏操作通过。 |
| 倒计时 | 自定义 1 分钟实际运行至自动到期；完成面板显示 00:01:00，不直接替用户标记完成。 |
| 计划提醒 | 保存当前时段 21:00–21:20 的计划，立即出现提醒；一键开始带入名称与“计划资料 P10”，完成后不重复提醒。 |
| 设置持久化 | 紧凑视图开启、刷新后关闭恢复原起始小时 0；天空主题及倒数日保存。 |
| 字号与配置重载 | 倒数日设为 48，重载配置、整页刷新、再次进入沉浸后，实际计算字号仍为 48px。 |
| 布局操作 | 专注收纳/展开；拖动时间轴分界、左右栏，实际计划比例变为 56%，侧栏宽度 407px。 |
| 窄屏 | 主界面底栏、沉浸录入、常驻暂停/结束、全屏详情底部操作、月历和统计，均实际检查。 |
| 遗留待清池 | 同源归并、完成/恢复、开始处理并完成、刷新后状态一致，见 B05。 |
| 日报生成 | 实际预览 37m、10 条记录及学科占比；计划时段不计入记录总时长。保存入口可点击，下载落盘待确认。 |
| 离线加载 | 停止 4186 HTTP 服务后刷新，仍加载 v25 页面、样式和既有测试记录，并可打开学科详情。 |

## 回归与剩余边界

- `node --test tests/requirements.test.cjs`：56 项通过，含原有 46 项和新增 10 项。模拟环境仍与真实页面验收分开统计。
- `node --check app.js`、`node --check sw.js`、资源/缓存版本一致性检查、`git diff --check` 通过。
- v25 在线交互检查未发现新的脚本错误；停止服务后的离线回退不应被误认为代码异常。
- 已询问用户是否允许在独立测试地址进行删除、清空、导入覆盖；未获确认前不执行。相关原有逻辑仍有隔离回归覆盖，但不能冒充浏览器实测。
- 悬浮小窗入口实际调用后成功退出沉浸回到主页，暂停能结束此次计时；小窗内部按钮、Edge 安装/卸载、Windows 通知授权、系统休眠与冻结没有完成系统级验收。
- 图片/JSON 下载落盘、备份导入往返、手机真机软键盘及真实跨午夜等待未作为已通过项。跨午夜逻辑有隔离测试；不会通过改动系统时间冒充实测。
- 之前“浏览器受访问限制”的表述不再适用；当前浏览器可访问，未完成项以上述具体范围为准。
